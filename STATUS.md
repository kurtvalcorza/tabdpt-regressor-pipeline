# Release status

Current status: **Candidate — standalone tutorial source complete, awaiting clean-runtime execution**. The pipeline package (three modules, all carried), the offline unit suite, `MODEL_CARD.md`, and the standalone tutorial pair — `tutorials/tabdpt_regressor_colab.ipynb` (`E2E`) and `tutorials/tabdpt_regressor_artifact_inference_colab.ipynb` (`ARTIFACT-INFERENCE`), both generated by `tools/build_notebook.py` under DIMER Notebook Specification 1.1 §3.6 — exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks for both notebooks). No clean-runtime execution of either notebook (previous or standalone pair) has been recorded; the execution-only gate was tracked in issue #15 and is now recorded in `docs/release-verification.md`. The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit of the standalone notebooks is recorded there.
